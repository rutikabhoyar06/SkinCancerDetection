streamlit==1.37.1
numpy==1.26.4
opencv-python==4.10.0.84
matplotlib==3.8.4
Pillow==10.4.0
scikit-learn==1.5.1
pydot==2.0.0
seaborn==0.13.2
deap==1.4.1
# Install Graphviz system package separately if plotting models (see README)
# Choose ONE of the following frameworks. Comment out the one you don't use.
# TensorFlow (CPU-only)
tensorflow==2.16.1
# Or, PyTorch (CPU-only); uncomment if you prefer PyTorch
# torch==2.4.0
# torchvision==0.19.0
